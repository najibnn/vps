name: SSH via ngrok (Persistent)

on: [workflow_dispatch]

jobs:
  ssh:
    runs-on: ubuntu-latest
    steps:
      - name: Install SSH server
        run: |
          sudo apt update
          sudo apt install openssh-server wget tar jq -y
          sudo mkdir -p /var/run/sshd

          # إنشاء مستخدم جديد بصلاحيات sudo
          sudo useradd -m sshuser
          echo "sshuser:123456" | sudo chpasswd
          sudo usermod -aG sudo sshuser

          # السماح بالاتصال بكلمة مرور
          sudo sed -i 's/#PasswordAuthentication yes/PasswordAuthentication yes/' /etc/ssh/sshd_config
          sudo sed -i 's/PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config

          # تشغيل SSH
          sudo systemctl enable ssh
          sudo systemctl restart ssh

      - name: Install latest ngrok v3
        run: |
          wget https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.tgz
          tar -xvzf ngrok-v3-stable-linux-amd64.tgz
          sudo mv ngrok /usr/local/bin/ngrok
          ngrok version

      - name: Configure ngrok and start TCP tunnel
        run: |
          ngrok config add-authtoken ${{ secrets.NGROK_AUTH }}
          nohup ngrok tcp 22 --log=stdout --log-format=json > ngrok_ssh.log &

      - name: Wait for ngrok TCP URL and display info
        run: |
          echo "Waiting for ngrok SSH info..."
          for i in {1..30}; do
            URL=$(jq -r 'select(.lvl=="info") | .url | select(. != null) | select(test("^tcp://"))' ngrok_ssh.log | head -n1)
            if [ ! -z "$URL" ]; then
              HOST=$(echo $URL | cut -d/ -f3 | cut -d: -f1)
              PORT=$(echo $URL | cut -d: -f3)
              echo "🔹 SSH is ready!"
              echo "Host / IP : $HOST"
              echo "Port      : $PORT"
              echo "User      : sshuser"
              echo "Password  : 123456"
              echo ""
              echo "Command to connect:"
              echo "ssh sshuser@$HOST -p $PORT"
              break
            fi
            sleep 2
          done

      - name: Keep Job Alive
        run: |
          echo "⚡ SSH + ngrok is running! Job will stay alive until manually stopped."
          tail -f /dev/null
